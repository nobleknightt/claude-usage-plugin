{
  "name": "claude-usage",
  "description": "Tracks per-user token usage, cost, and sessions for Claude Code, with support for teams sharing a single Claude account.",
  "version": "0.1.0",
  "author": {
    "name": "Ajay Dandge"
  },
  "userConfig": {
    "API_KEY": {
      "type": "string",
      "title": "API key",
      "description": "The API key generated for you in the tracker dashboard.",
      "sensitive": true,
      "required": true
    },
    "BASE_URL": {
      "type": "string",
      "title": "Tracker server URL",
      "description": "URL of the usage tracker server.",
      "required": true
    }
  }
}
