[project]
name = "server"
version = "0.1.0"
description = "Claude Code usage tracker — FastAPI + SQLite server and dashboard"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.138.2",
    "authlib>=1.3.2",
    "itsdangerous>=2.2.0",
    "joserfc>=1.7.4",
]
